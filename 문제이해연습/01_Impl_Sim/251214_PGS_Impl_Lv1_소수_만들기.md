# [251214_PGS_Impl_Lv1_소수_만들기]
> [문제 출처](https://school.programmers.co.kr/learn/courses/30/lessons/12977) <br/>

## 1. 🔍 분석 (10분)
- **제약 & 복잡도**:
   - N(nums) <= 50 -> O(N^3) # `수정됨` : 3개의 숫자를 뽑는 것이기에 N^2->N^3가 맞음
   - nums의 원소는 중복 없음
- **유형 & 자료구조**: list, Impl
- **핵심 로직 (Key Idea)**: 
   1. [전처리] 
      - 주어진 리스트를 3개씩 중복 없이 묶은 배열로 만들어줘야함.  
         - itertools.combinations(nums, 3) 사용 # `추가됨` : 조합 라이브러리 import itertools
   2. [메인 로직] (예외/탈출 조건 포함)
      - 3개씩 묶은 숫자의 내부 수의 합을 구함
         - 이중 리스트 내부 요소들을 mapping(sum) 수행
      - 소수 인지 판별(1~3000 이하의 자연수 중 소수인지 판별) # `수정됨` : 최대 1000의 숫자 3개의 합이기에 3000까지 고려. 
         - 내부 수의 합K 들을 2~K**0.5 로 나눈 뒤 나누어 떨어지지 않으면 소수
      - 소수면 answer +1
   3. [최종 결과] (return 값)
      - 3개의 숫자 배열 내부의 수를 더했을떄, 소수가 되는 경우의 개수
- **체크 포인트**:
   - [x] N=1 또는 0일 때 (최소 입력) --> 없음 

---

## 2. 📚 학습 (5분)
> 이 문제를 통해 새로 알게 된 점이나 다시 확인한 개념을 정리합니다.

**① 오늘의 알고리즘/개념**
> 개념의 핵심 원리 한 줄 요약
- 소수 판별 : 내부 수의 합K 들을 2~K**0.5 로 나눈 뒤 나누어 떨어지지 않으면 소수
- 조합 : itertools.combinations(nums, 3) 사용

**② Python Code Snippet (패턴 저장)**
> 나중에 써먹을 수 있는 핵심 코드 조각
```python
from itertools import combinations

comb = combinations(nums, 3)
```

---

## 💡 참고 시트
> 유형, 자료구조, 복잡도, 체크 포인트를 정리합니다.

1. 🎯 목표 복잡도 가이드 (N의 크기 ➜ 시간 복잡도)
   - N ≤ 20     ➜ O(2^N)  : 백트래킹, 재귀, 완전탐색
   - N ≤ 1,000  ➜ O(N^2)  : 2중 For문, DP, 2차원 배열
   - N ≤ 10만   ➜ O(NlogN): 정렬, 힙, 이분탐색, 다익스트라, 유니온파인드
   - N ≤ 100만  ➜ O(N)    : 투 포인터, 그리디, DP, 스택/큐, 해시
-------------------------------------------------------
2. 🧩 알고리즘 유형 (파일명 약어)
   - [기본] Impl(구현), Sim(시뮬레이션), BF(브루트포스, 완전탐색)
   - [탐색] BFS(너비우선), DFS(깊이우선), Back(백트래킹), BinSearch(이분탐색)
   - [최적화] Greedy(그리디), DP(동적계획법)
   - [그래프] Dijkstra(다익스트라), Floyd(플로이드워셜), Topo(위상정렬), MST
   - [기타] TwoPtr(투 포인터), Sliding(슬라이딩 윈도우), Prime(소수판별)
-------------------------------------------------------
3. 📦 자료구조 선택
   - [선형] List(배열), Stack(스택), Queue(큐), Deque(덱)
   - [비선형] Graph(인접리스트/행렬), Tree(트리), Heap(우선순위 큐)
   - [검색/집합] Set(집합), Dictionary(해시맵)
-------------------------------------------------------
4. ⚠️ 필수 체크 포인트 (Edge Case)
   - [ ] N=1 또는 0일 때 (최소 입력)
   - [ ] 데이터가 없는 경우 (Empty)
   - [ ] 모든 값이 동일한 경우
   - [ ] 정답이 음수이거나 0일 가능성
   - [ ] int 범위를 넘는 경우 (Python은 걱정 덜 해도 됨)
-------------------------------------------------------